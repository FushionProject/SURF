Describe the user-visible problem and resulting behavior.

Validation: list the offline checks run and any remaining release gates.

If stacked, link the prerequisite PR and explain the intended merge order.

Confirm that the diff and commit history contain only publishable source and fixtures. Keep credentials, environment files, provider archives, raw responses, customer data, caches and build outputs out of Git. Do not treat CI success as authorization to deploy or enable billing.
